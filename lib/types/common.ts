type DateType = {
  milliseconds: number;
  string: string;
};

type LocationType = {
  latitude: number;
  longitude: number;
  latitudeDelta: number;
  longitudeDelta: number;
};

export type { LocationType, DateType };
